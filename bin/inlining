#!/usr/bin/env node

"use strict";

var fs = require("fs");

var inlining = require("../index");

inlining(
  fs.readFileSync(process.argv[3], "utf8"),
  fs.readFileSync(process.argv[2], "utf8"),
  function (res) {
    console.log(res);
  }
);
