#!/usr/bin/env node

"use strict";

var fs = require("fs");

var inlining = require("../index");

var html = process.argv[2];

inlining(
  fs.readFileSync(html, "utf8"),
  html,
  function (res) {
    console.log(res);
  }
);
